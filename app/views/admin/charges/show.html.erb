<%= @charge.token%>
<%= Charge.human_enum_name(:status, @charge.status)%>
<%= @charge.company_token%>
<%= @charge.product_token%>
<%= @charge.customer_name%>
<%= @charge.customer_cpf%>
<%= @charge.payment_method%>
<%= @charge.regular_price%>
<%= @charge.discount_price%>
<%= link_to 'Ver recibo', receipt_path(@receipt.slug)%>